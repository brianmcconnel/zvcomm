name: zvcomm_cli
description: CLI for ZVComm PKI tooling and mesh simulator.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
resolution: workspace

dependencies:
  zvcomm_core:
    path: ../../packages/zvcomm_core
  zvcomm_pki:
    path: ../../packages/zvcomm_pki
  zvcomm_sim:
    path: ../../packages/zvcomm_sim
  args: ^2.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

executables:
  zvcomm: zvcomm_cli
