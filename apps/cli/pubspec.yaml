name: cli
description: CLI for ZVComm PKI tooling and mesh simulator.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
resolution: workspace

dependencies:
  core:
    path: ../../packages/core
  pki:
    path: ../../packages/pki
  sim:
    path: ../../packages/sim
  args: ^2.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

executables:
  zvcomm: cli
