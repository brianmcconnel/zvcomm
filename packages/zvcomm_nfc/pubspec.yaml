name: zvcomm_nfc
description: NFC transport for ZVComm (pairing / bootstrap and short payloads).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
resolution: workspace

dependencies:
  zvcomm_core:
    path: ../zvcomm_core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
