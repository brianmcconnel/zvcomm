name: zvcomm_ui
description: Shared Flutter UI widgets for ZVComm.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: '>=3.22.0'
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  zvcomm_core:
    path: ../zvcomm_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
