name: zvcomm_pki
description: Lightweight PKI tooling for ZVComm device identities and certificates.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
resolution: workspace

dependencies:
  zvcomm_core:
    path: ../zvcomm_core
  crypto: ^3.0.6

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
